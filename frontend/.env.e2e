# E2E environment — Vite dev server proxies API calls to the E2E backend (port 8001).
# Used automatically by `pnpm e2e` via the playwright webServer command.
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8001
VITE_AUTH_MODE=dev
VITE_DEV_BEARER_TOKEN=devtoken
