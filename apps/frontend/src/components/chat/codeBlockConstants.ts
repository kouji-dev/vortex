/** Plain `<pre>` while Shiki chunk / WASM loads (shared: Suspense fallback + ShikiCodeBlock). */
export const CODE_BLOCK_FALLBACK_PRE_CLASS =
  'my-2 overflow-x-auto rounded-md border border-neutral-200 bg-neutral-100 p-3 font-mono text-[13px] leading-relaxed text-neutral-800 dark:border-neutral-700 dark:bg-neutral-900 dark:text-neutral-200'
